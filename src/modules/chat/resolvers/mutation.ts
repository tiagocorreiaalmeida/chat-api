/* import { MutationResolvers } from '#Base/generated/typed-schema';
import { ChannelProvider } from '../providers/channel';

export const Mutation: MutationResolvers = {
  createChannel: (root, { data }, { injector }) =>
    injector.get(ChannelProvider).createChannel(data),
};
 */
